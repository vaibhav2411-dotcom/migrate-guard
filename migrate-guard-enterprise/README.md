# migrate-guard-enterprise

Enterprise-grade migration parity validator scaffold.

Quick start

1. Install dependencies:

```bash
cd migrate-guard-enterprise
npm install
npx playwright install chromium
```

2. Run in dev:

```bash
npm run dev -- --mapping ./mappings.csv
```
